import { Component } from '@angular/core';

@Component({
    selector: 'app',
    template: `
    <h1>
      this is App Component
    </h1>
  `
})

export class AppComponent {
    constructor() {}
}
